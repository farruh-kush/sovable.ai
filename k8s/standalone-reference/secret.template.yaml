# Template only. Do not commit real values.
# kubectl -n ai-routing-reference create secret generic runtime-secrets \
#   --from-literal=internal-secret="$(openssl rand -hex 32)" \
#   --from-literal=gateway-api-key="sk-replace-me" \
#   --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: runtime-secrets
  namespace: ai-routing-reference
type: Opaque
stringData:
  internal-secret: CHANGE_ME_BEFORE_DEPLOY
  gateway-api-key: REDACTED_MODEL_STUDIO_API_KEY
