# Frontend

**Author:** Farruh

This directory contains the production dashboard and documentation web surfaces. `dashboard/` is the Next.js static-export console served by Nginx. `frontend/docs-site/` is the MkDocs documentation site. The `legacy-*` directories preserve the earlier generated/template frontend and are not used by the production dashboard build.
