export default function UsageChart() { const points = [42,58,51,74,63,87,79,96,82,105,99,118]; const max = Math.max(...points); return <div className="chart"><div className="chart-bars">{points.map((value, index) => <div className="bar-col" key={index}><div className="bar" style={{ height: `${(value / max) * 100}%` }} /><span>{index + 1}</span></div>)}</div><div className="chart-legend"><span><i className="legend-dot gold" />Requests</span><strong>1,248 <small>this month</small></strong></div></div> }
