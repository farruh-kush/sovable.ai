# Engineering Tools

**Author:** Farruh

This directory contains repository maintenance utilities, patch files, and build helpers that are not runtime services. Build scripts are under `tools/build/`; reusable patch files are under `tools/patches/`.
