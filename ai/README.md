# AI Platform

**Author:** Farruh

This directory contains AI routing and provider policy assets. `config/routing.yaml` is the versioned configuration consumed by the router and deployment ConfigMap. Provider adapter code remains isolated under `microservices/provider`; routing decisions remain under `microservices/router`.
